services:
  - type: web
    name: brain-tumor-app
    env: python
    plan: free
    pythonVersion: 3.10.13          # Use this key instead of envVars for Python version
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
